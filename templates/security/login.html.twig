{% extends "security/base.html.twig"%}


{% block main %}
    <div class="wrapper login">
        <div class="row">
            <div class="col-xs-6 login-info">
                <div class="logo"><img src="/img/design/mar-logo.png" alt=""/>
                </div>
                <p>система управления вашим сайтом</p>
                <section></section>
            </div>
            <div class="col-xs-6 login-form">
                <div class="login-form__wrapp">
                    <h2>Вход в систему</h2>
                    <form class="form" action="{{ path('login') }}" method="post">
                        <div class="form-group">
                            <input type="text" name="_username">
                            <span class="form__placeholder">Имя</span>
                        </div>
                        <div class="form-group">
                            <input type="text" name="_password">
                            <span class="form__placeholder">Пароль</span>
                        </div>

                        <div class="form-group tac">
                            <button class="btn-form" type="submit"> Войти</button>
                        </div>
                    </form>
                </div>
            </div>
            <div class="login-decor"></div>
        </div>
    </div>

{% endblock %}

